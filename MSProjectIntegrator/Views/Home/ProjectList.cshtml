@model List<MSProjectIntegrator.Models.ProjectModel>
@{
    ViewBag.Title = "Projects List";
}

@*<div class="jumbotron">
        <h2>Projects</h2>
        <div class="col-md-12 input-header border-red"><label>@ViewBag.Title</label></div>

    </div>*@

<h2>@ViewBag.Title</h2>

<div class="row">
    <table class="table table-striped table-bordered table-hover dataTable" id="tablegrid">
        <thead>
            <tr>
                <th>Name</th>
                <th>Created Date</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody id="tUser">
            @{
                foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Name</td>
                        <td>@item.Created</td>
                        <td>
                            <input type="button" value="View" class="btn btn-warning btn-xs" onclick="View('@item.Id');" />
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>

<script>
    $(document).ready(function () {
        $('#tablegrid').DataTable({
            "processing": true,
            "searching": true,
            "ordering": true,
            "pagingType": "full_numbers"
        });
    });

    function View(projId) {

        window.location.href = '@Url.Content("~/Home/ProjectDetail?guid=")' + projId;
    }
</script>